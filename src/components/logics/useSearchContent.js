import React from "react";
import fuse from "fusejs";
/**
 * @param {Object} data - The data storage which is searched
 * @param {Object} options - The options for the searching process
 */
function useSearchContent(data, options) {
	return <div></div>;
}

export default useSearchContent;
